kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: allow-https-egress-api
spec:
  podSelector:
    matchLabels:
      app: ddc-bcss-test-subject-selector-api
  policyTypes:
  - Egress
  egress:
  - to: []
    ports:
    - protocol: TCP
      port: 443

