kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: allow-https-egress-app
spec:
  podSelector:
    matchLabels:
      app: ddc-bcss-test-subject-selector-app
  policyTypes:
  - Egress
  egress:
  - to: []
    ports:
    - protocol: TCP
      port: 443

